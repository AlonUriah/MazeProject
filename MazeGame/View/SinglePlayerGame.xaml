<Window x:Class="MazeGame.View.SinglePlayerGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MazeGame.View.Controls"
        xmlns:local="clr-namespace:MazeGame.View"
        mc:Ignorable="d"
        Title="SinglePlayerGame" Height="400" Width="300">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
        <TextBox HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="{Binding ElementName=game, Path=ActualHeight, Mode=OneWay}" VerticalAlignment="Top" Width="120"/>
        <TextBox HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="{Binding ElementName=game, Path=ActualWidth, Mode=OneWay}" VerticalAlignment="Top" Width="120"/>
        <controls:GameSurface x:Name="game" Margin="0,100,0,0"/>

    </StackPanel>
</Window>
